pybullet_sim:
  ros__parameters:
    physics_hz: 240.0
    publish_hz: 60.0
    seed: 42

trajectory_controller:
  ros__parameters:
    command_hz: 50.0
    goal_tolerance: 0.05
    settle_timeout: 2.0
