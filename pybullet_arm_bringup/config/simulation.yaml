pybullet_sim:
  ros__parameters:
    physics_hz: 240.0
    publish_hz: 60.0
    seed: 42
    cameras: true
    camera_hz: 10.0
    camera_width: 224
    camera_height: 224

trajectory_controller:
  ros__parameters:
    command_hz: 120.0
    interpolation: quintic
    goal_tolerance: 0.05
    settle_timeout: 2.0
